# Railway: prefer Dockerfile (see Dockerfile for Prisma-safe install)
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
